The skill's `var` field is empty (`var: ""`). Per the skill's instructions:

**autoresearch requires var= set to a skill name**

To run autoresearch, invoke it with a target skill name, e.g.:

```
skills/autoresearch/SKILL.md with var=heartbeat
```

Which skill would you like to evolve?
